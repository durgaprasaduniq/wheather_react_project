apiVersion: v1
kind: Service
metadata:
  name: service-node
  labels:
    env: app
    key: owner
spec:
  type: LoadBalancer
  selector:
    env: app
    key: owner
  ports:
    - port: 80
      targetPort: 80                                                                                                                                                         
